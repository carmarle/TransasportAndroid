<GridLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="dsic.online.patterns.DashboardActivity"
    android:columnCount="3"
    android:rowCount="2">

    <ImageButton
        android:id="@+id/bPlay"
        style="@style/dashboard_button"
        android:src="@mipmap/ic_millionaire_play_game"
        android:layout_width="150dp"
        android:maxWidth="150dp"
        android:minWidth="150dp"
        android:layout_height="150dp"
        android:maxHeight="150dp"
        android:minHeight="150dp"/>

    <ImageButton
        android:id="@+id/bScores"
        style="@style/dashboard_button"
        android:src="@mipmap/ic_millionaire_scores"
        android:layout_width="150dp"
        android:maxWidth="150dp"
        android:minWidth="150dp"
        android:layout_height="150dp"
        android:maxHeight="150dp"
        android:minHeight="150dp"/>

    <ImageButton
        android:id="@+id/bSettings"
        style="@style/dashboard_button"
        android:src="@mipmap/ic_millionaire_settings"
        android:layout_width="150dp"
        android:maxWidth="150dp"
        android:minWidth="150dp"
        android:layout_height="150dp"
        android:maxHeight="150dp"
        android:minHeight="150dp"/>

</GridLayout>
